<?xml version="1.0" encoding="utf-8"?>
<!-- Debug builds may talk plain HTTP (local server / emulator). Release builds refuse cleartext. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:usesCleartextTraffic="true"
        tools:replace="android:usesCleartextTraffic">
        <!-- Debug-only harness to inspect the group-call grid layout without live peers. -->
        <activity
            android:name=".feature.call.CallGridDebugActivity"
            android:exported="true"
            android:theme="@style/Theme.Klic" />
    </application>

</manifest>
